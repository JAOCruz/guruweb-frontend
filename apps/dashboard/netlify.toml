[build]
  command = "npm install && npm run build -w apps/dashboard"
  publish = "apps/dashboard/dist"

[build.environment]
  NODE_VERSION = "20.11.1"
  NPM_FLAGS = "--no-package-lock"
  CI = "false"
